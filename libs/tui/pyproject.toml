[project]
name = "tui"
version = "0.1.0"
description = "Haiku interactive terminal UI"
requires-python = ">=3.11"
dependencies = [
    "rich>=13.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tui"]
